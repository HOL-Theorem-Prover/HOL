CLINE_OPTIONS = --no_overlay
ifdef POLY
CLINE_OPTIONS += --poly_not_hol

DEPS = corpus_diff.sml ../util/GetOpt.sig ../util/GetOpt.sml

corpus-diff: poly-corpus-diff.ML $(DEPS)
	$(POLYC) -o $@ $<
endif

EXTRA_CLEANS = corpus-diff
