ifdef POLY
HOLHEAP = $(HOLDIR)/bin/hol.state0
endif

rebuild_cachekey-selftest.log: selftest.exe
	@$(tee ./$<, $@)

selftest.exe: selftest.uo
	$(HOLMOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe rebuild_cachekey-selftest.log
