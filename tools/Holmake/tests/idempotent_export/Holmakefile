ifdef POLY
HOLHEAP = $(HOLDIR)/bin/hol.state0
endif

idempotent_export-selftest.log: selftest.exe
	@$(tee ./$<, $@)

selftest.exe: selftest.uo
	$(HOLMOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe idempotent_export-selftest.log
