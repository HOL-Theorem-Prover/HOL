ifdef POLY
HOLHEAP = $(HOLDIR)/bin/hol.state0
endif

local-parallelism-limit-selftest.log: selftest.exe
	@$(tee ./$<, $@)

selftest.exe: selftest.uo
	$(HOLMOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe local-parallelism-limit-selftest.log \
               capped1/.plimit.lock capped1/.plimit.count \
               capped2/.plimit.lock capped2/.plimit.count
