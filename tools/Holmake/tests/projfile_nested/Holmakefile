OPTIONS = NO_OVERLAY
ifdef POLY
CLINE_OPTIONS = --poly_not_hol
endif

projfile_nested-selftest.log: selftest.exe
	$(tee ./$<, $@)

selftest.exe: selftest.uo
	$(MOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe projfile_nested-selftest.log
