ifdef POLY
HOLHEAP = $(HOLDIR)/bin/hol.state0
endif

placeholder_regroup-selftest.log: selftest.exe
	@$(tee ./$<, $@)

selftest.exe: selftest.uo
	$(HOLMOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe placeholder_regroup-selftest.log
