ifdef POLY
HOLHEAP = $(HOLDIR)/bin/hol.state0
endif

retry-oos-selftest.log: selftest.exe
	@$(tee ./$<, $@)

selftest.exe: selftest.uo
	$(HOLMOSMLC) -o $@ $<

EXTRA_CLEANS = selftest.exe retry-oos-selftest.log \
               transient/.oos.count persistent/.oos.count \
               otherfail/.oos.count
