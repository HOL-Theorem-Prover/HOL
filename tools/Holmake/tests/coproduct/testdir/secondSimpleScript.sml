open HolKernel Parse boolLib

open localbaseTheory baseTheory

val _ = new_theory "secondSimple";

val _ = let
  val ostrm = TextIO.openOut "bar"
in
  TextIO.output(ostrm, "Generated by secondSimpleScript.sml\n");
  TextIO.closeOut ostrm
end;

val TQQ = store_thm(
  "TQQ",
  ``T ==> (Q ==> Q)``,
  STRIP_TAC THEN ACCEPT_TAC Q_implies_Q);

val _ = export_theory();
