\DOC list_mk_pabs

\TYPE {list_mk_pabs : ((term list # term) -> term)}

\LIBRARY pair

\SYNOPSIS
Iteratively constructs paired abstractions.

\DESCRIBE
{list_mk_pabs(["p1";...;"pn"],"t")} returns {"\p1 ... pn. t"}.

\FAILURE
Fails with {list_mk_pabs} if the terms in the list are not paired 
structures of variables.

\COMMENTS
The system shows the type as {goal -> term}.

\SEEALSO
list_mk_abs, strip_pabs, mk_pabs.

\ENDDOC
