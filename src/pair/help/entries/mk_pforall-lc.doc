\DOC mk_pforall

\TYPE {mk_pforall : ((term # term) -> term)}

\LIBRARY pair

\SYNOPSIS
Constructs a paired universal quantification.

\DESCRIBE
{mk_pforall("pair","t")} returns {"!pair. t"}.

\FAILURE
Fails with {mk_pforall} if first term is not a a paired structure of variables
or if {t} is not of type {":bool"}.

\SEEALSO
mk_forall, dest_pforall, is_pforall, list_mk_pforall.

\ENDDOC
