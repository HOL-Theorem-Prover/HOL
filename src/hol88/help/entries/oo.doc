\DOC oo

\TYPE {Compat.oo :('a * 'b -> 'c) * (('d -> 'a) * ('d -> 'b)) -> 'd -> 'c}

\SYNOPSIS
An infix operator that composes a function over a pair of functions:
{(f oo (g,h)) x} = {f (g x, h x)}.

\KEYWORDS
combinator.

\FAILURE
Never fails, unless the {hol88} library has not been loaded.

\SEEALSO
##, B, C, CB, Co, I, K, KI, o, S, W.

\ENDDOC
