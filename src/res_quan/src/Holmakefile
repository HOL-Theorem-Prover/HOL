ifdef POLY
HOLHEAP = ../../num/termination/numheap
endif

all: $(DEFAULT_TARGETS)

ifeq ($(KERNELID),otknl)
ARTFILES = $(patsubst %Script.sml,%.ot.art,$(wildcard *Script.sml))

all: $(ARTFILES) hol4-res-quan.art

hol4-res-quan.art: hol4-res-quan.thy ../../opentheory/hol4.int
	opentheory info --article -o $@ $<
	opentheory install --reinstall hol4-res-quan.thy
endif

ifdef HOLBUILD
all: link-to-sigobj

link-to-sigobj: $(DEFAULT_TARGETS)
	$(HOL_LNSIGOBJ)

.PHONY: link-to-sigobj
endif

.PHONY: all
