% =====================================================================
% HOL Manual LaTeX Source: tutorial
% =====================================================================

\documentclass[12pt,fleqn]{book}
\input{book-title}    % defines \HOLbookTitle from book.toml's title
\usepackage[notindex]{tocbibind}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{alltt}
\usepackage{../LaTeX/layout}
\usepackage{graphicx}
\usepackage{../Description/desc-unicode}  % HOL Unicode codepoints
                                          % shared with Description.
\usepackage[hidelinks,hypertexnames=false]{hyperref}
\hypersetup{pdftitle={\HOLbookTitle}}
%HEVEA \usepackage{epsfig}

\renewcommand\leadsto\rightarrow

% ---------------------------------------------------------------------
% Input defined macros and commands
% ---------------------------------------------------------------------
\input{../LaTeX/commands}
\input{../LaTeX/pic-macros}
%\input{../LaTeX/docmacros}
\usepackage{url}
\usepackage[line,arrow,frame,matrix]{xy}
\usepackage{tikz}
\usetikzlibrary{arrows,positioning}
% Standalone lets \input{figs/<x>} pull in standalone-class TikZ
% sources (which compile by themselves to PDF -> SVG for the
% mdbook side) into the main document, stripping their preambles.
\usepackage{standalone}
\usepackage{../LaTeX/proof}

% Packages and definitions emitted (or referenced) by
% pandoc-generated .tex from .smd chapters.
\usepackage{array}
\usepackage{longtable}
\usepackage{booktabs}
\usepackage{calc}
\providecommand{\tightlist}{% pandoc fragments emit this for tight lists
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\newcounter{none}                 % referenced by pandoc longtable captions

%\includeonly{title,contents,preface,../LaTeX/ack,intro,ml,logic,
% proof,references,Studies/preface,Studies/parity/parity}
%             Studies/microprocessor/all,
%\includeonly{Studies/microprocessor/all}
%\includeonly{Studies/int_mod/mod_arith_study/tutorial}
%\includeonly{binomial}
%\includeonly{parity}
% Pre-declare `\US_cite` so underscore's `\do\cite` step skips its
% protection wrapper for `\cite`.  Without this, underscore wraps
% optional-arg-accepting commands such that `\cite[#1]{#2}` puts
% `\cite[#1]` inside its own group, breaking natbib's parse and
% leaving `[` as the (undefined) cite key.  None of this manual's
% cite keys contain `_`, so we don't need cite-argument protection.
\expandafter\def\csname US_cite\endcsname{}
\usepackage[strings]{underscore}


\begin{document}

   \setlength{\unitlength}{1mm}           % unit of length = 1mm
   \setlength{\baselineskip}{16pt}        % line spacing = 16pt

   % ---------------------------------------------------------------------
   % prelims
   % ---------------------------------------------------------------------

%   \pagenumbering{roman}                  % roman page numbers for prelims
%   \setcounter{page}{1}                   % start at page 1

   \include{title}                        % tutorial title page
   \include{preface}                      % preface to entire tutorial
   \input{../LaTeX/ack}                 % global acknowledgements
   \tableofcontents

%   \newpage
%   \pagenumbering{arabic}                % arabic page numbers
%   \setcounter{page}{1}                  % start at page 1

   % Active chapters are listed in chapters.txt and stitched in
   % here by chapters-include.tex (regenerated by gen_chap_lists).
   % Commented-out chapters (logic, tool, binomial, conv) remain
   % excluded.
   \input{chapters-include.tex}

   % The hand-rolled references.tex this replaces listed every entry
   % unconditionally via \bibitem, regardless of which chapters were
   % active.  Several of those entries are only cited from currently-
   % commented-out chapters (logic, tool, binomial); \nocite{*}
   % preserves that "list everything in the .bib" behaviour.
   \nocite{*}
   \bibliographystyle{plainnat}
   \bibliography{tutorial}

\end{document}
