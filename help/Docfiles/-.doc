\DOC -

\TYPE {$- : ((int # int) -> int)}

\SYNOPSIS
Performs subtraction on ML integers.

\KEYWORDS
arithmetic, number.

\FAILURE
Never fails.

\COMMENTS
Unary {-} exists as an internal parser object, but not as a function.  So,
whilst typing in {-1;;} will work, {-;;} will return a parse error.

\ENDDOC
