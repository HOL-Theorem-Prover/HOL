\DOC mk_abs

\TYPE {mk_abs : term * term -> term}

\SYNOPSIS
Constructs an abstraction.

\DESCRIBE
{mk_abs (v, t)} returns the abstraction {\v. t}.

\FAILURE
Fails if {v} is not a variable.

\SEEALSO
dest_abs, is_abs, list_mk_abs, mk_var, mk_const, mk_comb.

\ENDDOC
