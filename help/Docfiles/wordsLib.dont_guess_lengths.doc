\DOC 

\TYPE {dont_guess_lengths : unit -> unit}

\SYNOPSIS

Turns off word length guessing.

\DESCRIBE

Turns off the word length guessing initiated by {guess_lengths}.

\EXAMPLE
{
- load "wordsLib";
...
- show_types := true;
> val it = () : unit
- wordsLib.guess_lengths();
> val it = () : unit
- wordsLib.dont_guess_lengths();
> val it = () : unit
- ``(7 >< 5) a @@ (4 >< 0) a``;
<<HOL message: inventing new type variable names: 'a, 'b, 'c, 'd>>
> val it =
    ``(((7 :num) >< (5 :num)) (a :bool['d]) :bool['a]) @@
      (((4 :num) >< (0 :num)) a :bool['b])`` : term
}

\SEEALSO

wordsLib.guess_lengths, wordsLib.guess_word_lengths, wordsLib.notify_word_length_guesses

\ENDDOC
