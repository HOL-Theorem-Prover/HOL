>>__ Parse.temp_set_grammars $ valOf $ grammarDB {thyname="hol"};
## `config`

``` hol4
Refute.type config

Refute.default_qc_config : qc_config
Refute.default_mf_config : mf_config
Refute.default_config : config
Refute.the_config : config ref
Refute.show_config : unit -> unit
Refute.apply_updates : config_update list -> config -> config
Refute.current_config : config_update list -> config
```

------------------------------------------------------------------------

Configures Refute searches.

A `config` is an immutable record.  Updaters take a new field value and
return a function from the old configuration to the new one, so they are
normally composed with `|>`.  `default_config` contains
`default_qc_config` and `default_mf_config`.  `the_config` is the mutable
interactive default used by `quickcheck`, `model_refute`, `REFUTE_TAC`,
`QUICKCHECK_TAC`, `NARROWING_TAC`, and `MODEL_REFUTE_TAC`.
`show_config ()` prints the complete value of `!the_config`.  The tactics
read `the_config` when they are applied and honour every field, including
`quiet` and `expect`; each returns the original goal unchanged.
`apply_updates updates cfg` applies a list of updaters to `cfg` from
left to right, and `current_config updates` applies the same list to
`!the_config`; these are what `REFUTE_TAC_WITH`, `refute_with`, and
`refute_goal_with` are built from.

``` hol4
val cfg = Refute.default_config
  |> Refute.upd_timeout 5.0
  |> Refute.upd_search
       (Refute.Only [Refute.Exhaustive, Refute.Narrowing])
  |> Refute.upd_size 7
```

### Common fields

``` hol4
Refute.upd_timeout : real -> config -> config
Refute.upd_search : search -> config -> config
Refute.upd_sequential : bool -> config -> config
Refute.upd_genuine_only : bool -> config -> config
Refute.upd_abort_potential : bool -> config -> config
Refute.upd_quiet : bool -> config -> config
Refute.upd_no_assms : bool -> config -> config
Refute.upd_evals : term list -> config -> config
Refute.upd_expect : expectation -> config -> config
Refute.upd_max_counterexamples : int -> config -> config
Refute.upd_tag : string -> config -> config
Refute.upd_widths : int list -> config -> config
Refute.upd_qc : qc_config -> config -> config
Refute.upd_mf : mf_config -> config -> config
```

| Field | Default | Meaning |
|---|---:|---|
| `timeout` | `10.0` | Seconds in the whole-call search deadline. |
| `backends` | `NONE` | All backends, or exactly the names in `SOME`. |
| `sequential` | `false` | Run backends by weight instead of racing them. |
| `genuine_only` | `false` | Request only genuine hits. |
| `abort_potential` | `false` | Stop at the first hit of any certainty. |
| `quiet` | `false` | Suppress traces, messages, and warnings. |
| `no_assms` | `false` | Ignore assumptions passed to `refute_goal`. |
| `evals` | `[]` | Reconstruct and display these additional terms. |
| `expect` | `NoExpectation` | Check a regression expectation after the run. |
| `max_counterexamples` | `1` | Maximum number of QC hits requested. |
| `tag` | `""` | Append text to the formatted outcome. |
| `widths` | `[1,2,3,4]` | Widths tried for a word type variable. |

The `timeout` is one absolute deadline for the whole call: preprocessing,
backend admission, compilation, testing, and solver calls all consume the
same remainder.  The `backends` field is set through `upd_search`, which
takes the typed `AllBackends`, `QuickcheckBackends`, or `Only choices`
selection.

The expectation constructors are `NoExpectation`, `ExpectNone`,
`ExpectUnknown`, `ExpectCex`, `ExpectModel`, `ExpectNoModel`,
`ExpectGenuine`, `ExpectQuasiGenuine`, and `ExpectPotential`.  A mismatch
raises a HOL error.  A word type variable takes its instances from
`widths`, independently of the QC and MF sub-records.  `upd_qc` and `upd_mf`
replace a complete sub-record; the individual updaters are usually less
brittle.

### Quickcheck and narrowing fields

``` hol4
Refute.upd_size : int -> config -> config
Refute.upd_iterative_size : int -> config -> config
Refute.upd_iterations : int -> config -> config
Refute.upd_depth : int -> config -> config
Refute.upd_finite_types : bool -> config -> config
Refute.upd_finite_type_size : int -> config -> config
Refute.upd_default_type : hol_type list -> config -> config
Refute.upd_instantiate :
  (hol_type option * hol_type) list -> config -> config
Refute.upd_substrate : substrate_choice -> config -> config
Refute.upd_seed : int option -> config -> config
Refute.upd_allow_existentials : bool -> config -> config
Refute.upd_finite_functions : bool -> config -> config
Refute.upd_certify : bool -> config -> config
Refute.upd_smart_quantifier : bool -> config -> config
Refute.upd_smart_generators : bool -> config -> config
Refute.upd_optimise_equality : bool -> config -> config
Refute.upd_reorder_premises : bool -> config -> config
Refute.upd_use_subtype : bool -> config -> config
Refute.upd_allow_function_inversion : bool -> config -> config
```

| Field | Default | Meaning |
|---|---:|---|
| `size` | `10` | Maximum generated size and narrowing refinement depth. |
| `size_mode` | iterative | `upd_size` pins a fixed bound instead. |
| `iterations` | `100` | Number of random draws. |
| `depth` | `10` | Fixed smart-enumerator fuel for each plan. |
| `finite_types` | `true` | Add small finite type instances. |
| `finite_type_size` | `3` | Number of finite cardinalities to consider. |
| `default_type` | `[``:num``]` | Instances if `finite_types` is false. |
| `instantiate` | `[]` | Per-type-variable pins overriding `finite_types`/`default_type`. |
| `substrate` | `Auto` | `Auto`, `NativeSML`, `Cv`, or `Compute`. |
| `seed` | `NONE` | Seed; `NONE` advances the session stream. |
| `allow_existentials` | `true` | Permit PNF existential narrowing. |
| `finite_functions` | `true` | Finitize narrowing function inputs. |
| `certify` | `true` | Independently certify QC and narrowing hits. |
| `smart_quantifier` | `true` | Compile premises into plans. |
| `smart_generators` | `true` | Enable Horn `Enum` plans. |
| `optimise_equality` | `true` | Bind suitable equality premises. |
| `reorder_premises` | `true` | Reorder premises during smart mode inference. |
| `use_subtype` | `false` | Test a generator-less typedef by transporting to its representation type. |
| `allow_function_inversion` | `false` | Invert an `f ... = res` premise via its graph clauses. |

The default `size_mode` is iterative deepening: `10` is an initial
window, not a terminal bound, and testing deepens until the deadline.
`upd_size` pins a fixed, reproducible bound, while `upd_iterative_size`
sets a different initial window and restores adaptive deepening.

`depth` is live: it is the fuel supplied to each smart enumerator, not a
second scheduled search from zero to `depth`.  `allow_existentials`
controls selection of the PNF narrowing engine, and `finite_functions`
controls update-chain or constant-function representations there.
Setting `certify` false skips QC evaluation and replay certification;
a hit remains `Genuine` but has `cert = NONE`.
`allow_function_inversion` is itself a smart-generator route, so it also
requires `smart_generators` (default `true`): with smart generators off,
setting it has no effect.

`substrate` selects, it does not promise: under `Auto` a substrate that
declines the goal is skipped.  `Cv` declines every goal a tactic reaches
from the goalstack (`e`, `expandf`), because its translation defines
generators through the proof manager the goalstack tactic is already
holding, so `Auto` uses `Compute` there; selecting `Cv` explicitly
reports `Unknown` instead of switching substrate, as for any other
inapplicability.

By default QC substitutes one type for every type variable of a goal at
once, so a goal with two type variables is tested with both at the same
type.  `upd_instantiate` pins named type variables to fixed types
instead: a `SOME ty` key pins that type variable, and a variable no
entry reaches keeps taking the shared `finite_types`/`default_type`
carrier, exactly as `[]` would.  A `NONE` key is the fallback for every
*non-width* variable no `SOME` entry names, so a `NONE`-only row pins
all of them and, like a fully-`SOME` assignment, collapses the search to
a single instance -- unless the goal has a symbolic word width, since a
width variable takes a pin only from its own `SOME` entry (`NONE` never
reaches it: carriers and word-width numerals are disjoint value spaces)
and otherwise keeps varying over the configured widths.  A type variable
in a word's index position must therefore be pinned to a concrete
word-width type (`:1`, `:2`, ...), not an ordinary carrier.  Naming a
type variable the goal does not have, naming a width variable with a
non-width type, or naming a pin value that still carries type variables
of its own, is a configuration error reported as `Unknown`, not a
silent fallback.  Pinning changes only which monomorphic proxies QC
tests: a still-polymorphic goal whose pinned instances are completely
covered still degrades to `Unknown`, never `NoCounterexample`.
`upd_instantiate` scopes to QC and narrowing; the model finder is
unaffected and has its own per-type-variable updater,
`upd_card [(SOME ty, [n])]`.

A free variable at a typedef type `t` (`new_type_definition`, abstraction
`abs`, representation type `rty`, characteristic predicate `P`) usually
leaves QC with nothing to test: `t` has no generator and none of the
built-in fallbacks apply.  `upd_use_subtype true` rewrites `!x : t. B x`
to `!r : rty. P r ==> B (abs r)`, contracting every `rep (abs r)` redex
the substitution creates back to `r`, and reports a hit's binding for
`x` as `abs`-of-the-tested `r`, never `r` itself.  `TYPE_DEFINITION P
rep` makes `abs` restricted to `{r | P r}` a bijection onto `t`, so the
guard is the soundness condition, not an optimisation: dropping it lets
a representation value outside `t`'s image manufacture a spurious
refutation.  Whether a filtered representation search counts as
exhausted follows the same completeness bookkeeping as any other QC
search; transport adds no bespoke exhaustion rule in either direction.
In practice that makes transport a counterexample-search aid rather
than a verification route: the generator ranges over the whole
representation type and does not know the characteristic predicate
bounds it, so a true transported goal reports `Unknown` unless its body
collapses to a ground truth under normalization.
A genuine transported hit never certifies: replaying the original
closure needs the untransported `x`, but a transported search's
environment only ever binds the representation-typed `r`, so the
report always carries `Genuine` certainty with no attached certificate
-- proving it by kernel replay would require evaluating the very
`rep (abs r)` redex the contraction exists to avoid.
Transport is a fallback of last resort: a registered generator or an
`abstract_generator` registration for `t` wins, a TypeBase datatype for
`t` wins, and only when none of those apply and `t` is itself a
harvestable typedef with a real `TYPE_DEFINITION` theorem behind it
does the flag take effect.  That last requirement excludes the model
finder's synthetic frac and fmap typedef entries, which are an
encoding device for the model finder and carry no such theorem, so
nothing licenses the guard or the contraction for them.  It also does not reach
inside another type: a free variable of type `t list` keeps today's
`NoGenerator`/`Unknown` behaviour, since transporting it would require
lifting the predicate and generator through the list structure, which
this fallback does not do.  Default `false`, matching Isabelle's
`use_subtype`; the model finder never sees the rewrite; it already
handles typedefs natively.

### Model-finder fields

``` hol4
Refute.upd_card :
  (hol_type option * int list) list -> config -> config
Refute.upd_iterative_card :
  (hol_type option * int list) list -> config -> config
Refute.upd_max : (term option * int list) list -> config -> config
Refute.upd_mono :
  (hol_type option * bool option) list -> config -> config
Refute.upd_wf :
  (term option * bool option) list -> config -> config
Refute.upd_sat_solver : string -> config -> config
Refute.upd_batch_size : int -> config -> config
Refute.upd_falsify : bool -> config -> config
Refute.upd_user_axioms : bool option -> config -> config
Refute.upd_destroy_constrs : bool -> config -> config
Refute.upd_total_consts : bool option -> config -> config
Refute.upd_peephole_optim : bool -> config -> config
Refute.upd_datatype_sym_break : int -> config -> config
Refute.upd_kodkod_sym_break : int -> config -> config
Refute.upd_max_potential : int -> config -> config
Refute.upd_max_genuine : int -> config -> config
Refute.upd_atoms :
  (hol_type option * string list) list -> config -> config
Refute.upd_format :
  (term option * int list) list -> config -> config
Refute.upd_show_types : bool -> config -> config
Refute.upd_show_skolems : bool -> config -> config
Refute.upd_show_consts : bool -> config -> config
Refute.upd_debug : bool -> config -> config
Refute.upd_overlord : bool -> config -> config
Refute.upd_max_threads : int -> config -> config
Refute.upd_tac_timeout : real -> config -> config
Refute.upd_specialize : bool -> config -> config
Refute.upd_box :
  (hol_type option * bool option) list -> config -> config
Refute.upd_binary_ints : bool option -> config -> config
Refute.upd_bits : int list -> config -> config
Refute.upd_star_linear_preds : bool -> config -> config
Refute.upd_iter :
  (term option * int list) list -> config -> config
Refute.upd_bisim_depth : int list -> config -> config
Refute.upd_finitize :
  (hol_type option * bool option) list -> config -> config
Refute.upd_whack : term list -> config -> config
Refute.upd_need : term list option -> config -> config
Refute.upd_merge_type_vars : bool -> config -> config
```

| Field | Default | Meaning |
|---|---:|---|
| `card` | fallback `[1,...,10]` | Finite cardinalities by type. |
| `card_mode` | iterative | `upd_card` pins fixed rows instead. |
| `max` | fallback `[~1]` | Constructor multiplicities; `~1` is automatic. |
| `mono` | fallback `NONE` | Automatic or forced monotonicity by type. |
| `wf` | fallback `NONE` | Automatic or forced well-foundedness by constant. |
| `sat_solver` | `"smart"` | Kodkodi SAT solver selection. |
| `batch_size` | `50` | Scope problems submitted per solver batch. |
| `falsify` | `true` | Falsify; false searches for a model. |
| `user_axioms` | `NONE` | Automatic or forced inclusion of user axioms. |
| `destroy_constrs` | `true` | Simplify constructor applications. |
| `total_consts` | `NONE` | Automatic or forced total encoding of constants. |
| `peephole_optim` | `true` | Enable relational peephole optimization. |
| `datatype_sym_break` | `5` | Datatype symmetry-breaking strength. |
| `kodkod_sym_break` | `15` | Kodkod symmetry-breaking limit. |
| `max_potential` | `1` | Global budget for potential models. |
| `max_genuine` | `1` | Global budget for genuine models. |
| `atoms` | fallback `[]` | Preferred printed atom names by type. |
| `format` | fallback `[1]` | Tuple curried arguments for display. |
| `show_types` | `false` | Include interpreted type universes. |
| `show_skolems` | `true` | Include Skolem values. |
| `show_consts` | `false` | Include reconstructed constant tables. |
| `debug` | `false` | Enable MF debug mode and one-problem batches. |
| `overlord` | `false` | Keep solver files under stable local names. |
| `max_threads` | `0` | Kodkodi thread cap; zero leaves it unspecified. |
| `tac_timeout` | `0.5` | Seconds for inner MF tactics. |
| `specialize` | `true` | Specialize static higher-order arguments. |
| `box` | fallback `NONE` | Choose or force boxing. |
| `binary_ints` | `NONE` | Automatically or forcibly use binary integers. |
| `bits` | `[1,...,10]` | Candidate binary word widths. |
| `star_linear_preds` | `true` | Exact linear-predicate closure. |
| `iter` | fallback depth row | Fixpoint-unrolling depths. |
| `bisim_depth` | `[9]` | Codatatype depths; `~1` disables bisimulation. |
| `finitize` | fallback `NONE` | Choose or force finitization. |
| `whack` | `[]` | Definitions preprocessing should ignore. |
| `need` | `NONE` | Terms whose values must occur in a model. |
| `merge_type_vars` | `false` | Merge MF type-variable scopes. |

The default `card_mode` is likewise iterative: the fallback row is an
initial window that keeps growing until the deadline.  `upd_card` pins
fixed rows, while `upd_iterative_card` restores adaptive scanning; an
iterative row must be a consecutive prefix `[1, ..., n]`.

For row-valued fields, a `NONE` key is the fallback and a `SOME` key is
an override.  In a `format` row, the positive integers group consecutive
curried arguments into tupled arguments for model display.  Thus `[2]`
groups the final two arguments, while any earlier arguments remain
curried; multiple integers describe consecutive groups from left to
right within the grouped suffix.  The fallback `[1]` leaves unmatched
functions' curried argument grouping unchanged.  Boolean row values use
`NONE` for automatic choice and `SOME true` or `SOME false` to force it.
The default `iter` row is
`[0,1,2,4,8,12,16,20,24,28]`; a mutual fixpoint group shares one row,
with cases-theorem order and the first explicit row deciding it.
`merge_type_vars` can reduce the scope product but can lose models that
need different cardinalities for the original variables.

Updaters validate the resulting record.  `widths` must be nonempty with
every entry between 1 and 32.  MF updaters validate the MF sub-record; in
particular, `bits` must be nonempty and between 1 and 31.  Every `iter`
row must be nonempty, nonnegative, and have a representable successor; every
nonnegative `bisim_depth` must likewise have a representable successor.
A `bisim_depth` row cannot mix `~1` (disabled) with nonnegative depths.
The non-fallback keys of `max`, `wf`, and `iter` must be constants or
variables, and of `instantiate` a type variable.  `max_genuine` must be
positive and `max_potential` nonnegative.

### See also

[`Refute.refute`](#Refute.refute),
[`Refute.refute_goal`](#Refute.refute_goal),
[`Refute.REFUTE_TAC`](#Refute.REFUTE_TAC),
[`Refute.QUICKCHECK_TAC`](#Refute.QUICKCHECK_TAC),
[`Refute.NARROWING_TAC`](#Refute.NARROWING_TAC),
[`Refute.MODEL_REFUTE_TAC`](#Refute.MODEL_REFUTE_TAC),
[`Refute.REFUTE_TAC_WITH`](#Refute.REFUTE_TAC_WITH),
[`Refute.REFUTE_CONFIG_TAC`](#Refute.REFUTE_CONFIG_TAC),
[`Refute.quickcheck`](#Refute.quickcheck),
[`Refute.model_refute`](#Refute.model_refute),
[`Refute.try_refute`](#Refute.try_refute),
[`Refute.find_unused_assms`](#Refute.find_unused_assms)

The complete library guide is \ref{Description:the-holrefute-library}.
