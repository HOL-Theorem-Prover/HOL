tl;

/* list(i_DATA,i) */
list_reverse(i;) [list(i)] {
  local p, x;
  p = NULL;
  /*exists i1_DATA i2_DATA.
    i_DATA = APPEND i1_DATA i2_DATA &
    list(i2_DATA,i) * list(reverse(i1_DATA),p)*/
  while (i != NULL) [list(i) * list(p)] {
    x = i->tl;
    i->tl = p;
    p = i;
    i = x;
  }
  i = p;
} [list(i)]
/* list(reverse(i_DATA),i) */