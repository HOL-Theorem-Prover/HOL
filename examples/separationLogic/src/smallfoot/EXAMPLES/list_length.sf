tl;

/*[list(c_DATA,c)]*/
list_length(r;c) [list(c)] {
  local t;
  if (c == NULL) {
    r = 0;
  } else {
    t = c->tl;
    list_length(r;t);
    r = r + 1;
  }
} [list(c)]
/*[list(c_DATA,c) * (r == length(c_DATA))]*/
