NOTE: See example.lisp for explanatory comments.

==============================

Contents of this directory:

example.lisp
Example source file.

example.lisp.trans
Generated from example.lisp by this command:
../filter-forms.csh example.lisp example.lisp.trans

example.lisp.ml
Generated by this command:
../a2ml.csh example.lisp.trans example.ml

==============================

NOTE: example.lisp.trans has forms that are calls of a fictitious function,
encap, e.g.:

(ENCAP ((H1 (X1)) (H2 (X1 X2)))
       (DEFTHM H1-PROP
               (IMPLIES (INTEGERP X)
                        (INTEGERP (H1 X))))
       (DEFTHM H2-PROP
               (IMPLIES (INTEGERP Y)
                        (INTEGERP (MV-NTH '0 (H2 X Y))))))

The first argument of encap is a list of signatures: so here h1 takes one
argument and h2 takes two arguments.  The other arguments are definitions and
theorems.
