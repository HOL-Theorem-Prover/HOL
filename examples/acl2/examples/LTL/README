This directory will eventually contain a revised version of the case
study in:

 HOL/examples/acl2/examples/acl2-hol-ltl-paper-example

The directory above is to support a paper on the example. This
directory (examples/LTL) will just contain the case study material
and will point to general infrastructure files, rather than contain
them (as acl2-hol-ltl-paper-example does).

