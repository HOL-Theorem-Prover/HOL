Package that contains classes to represent solvers.

Sub-package jsolver, implements the classes TermSystem and TermCSP
for the concrete solver JSolver.

Sub-package gecodej, implements the classes TermSystem and TermCSP
for the concrete solver GecodeJ (see http://www.gecode.org/gecodej/index.html).