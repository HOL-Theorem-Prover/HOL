A HOL4 JSON parser with arbitrary-precision numbers, conforming to the ECMA-404 and RFC 8259 grammar. It has been successfully validated against the full [JSONTestSuite](https://github.com/nst/JSONTestSuite).

`validation/validate.sh` can be used to perform the validation. Note that this tries to clone JSONTestSuite from GitHub.